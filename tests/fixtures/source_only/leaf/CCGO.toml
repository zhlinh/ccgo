[project]
name = "leaf"
version = "0.1.0"
description = "Source-only leaf dep — no prebuilt lib/"
group_id = "com.ccgo.test.leaf"
license = "MIT"

[build]
output_dir = "cmake_build"

[apple]
export_headers = [
    { src = "include/leaf.h", dest = "" },
]

[include]
src = "include"
